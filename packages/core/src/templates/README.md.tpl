# {{name}}

This repository was scaffolded by `harness init` on {{date}}.

Edit `AGENTS.md.template` and `.codex/config.toml.template`, then run `harness sync` to render the managed Codex files. Do not edit `AGENTS.md` or `.codex/config.toml` directly, and do not commit adapter projection outputs.

## Setup

1. Review and customize `harness.yaml`.
2. Update `AGENTS.md.template` and `.codex/config.toml.template`.
3. Run `harness sync`.
4. Run `harness diff` to confirm zero drift.
